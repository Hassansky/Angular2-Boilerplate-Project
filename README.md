Angular2-Boilerplate-Project
Is basic 'hello world' project for angular app created to easy work with all awesome features of gulp.

To build project you need run:
npm install
to install al dependencies.

gulp build to build app
gulp dev to run development server and all watchers to auto reload changes in browser.